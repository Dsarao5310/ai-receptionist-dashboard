import { BarChart3 } from "lucide-react";
import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export default function AnalyticsPage() {
  return (
    <PagePlaceholder
      icon={BarChart3}
      title="Analytics"
      description="Booking performance, intent distribution, peak contact times, and channel performance are coming in a later build phase."
    />
  );
}
