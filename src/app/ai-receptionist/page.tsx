import { Bot } from "lucide-react";
import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export default function AIReceptionistPage() {
  return (
    <PagePlaceholder
      icon={Bot}
      title="AI Receptionist"
      description="Status, greeting, voice, personality, and business-rule controls for your AI receptionist are coming in a later build phase."
    />
  );
}
